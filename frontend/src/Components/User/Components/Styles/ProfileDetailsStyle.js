import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    profileTabLink: {
        padding: '10px 20px',
        fontSize: '15px',
        color: '#696F79',
    },
    secondGrid: {
        width: "100%",
    }
}))